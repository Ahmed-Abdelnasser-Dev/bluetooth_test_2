name: bluetooth_test_2
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  flutter_blue_plus:
  permission_handler: 
  flutter_svg:
  http:
  intl:
  google_generative_ai:
  flutter_tts:
  speech_to_text:
  hl_image_picker:
  googleapis:
  googleapis_auth:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/imgs/
